[build]
  functions = "functions"
  publish = "."

[functions]
  external_node_modules = ["@netlify/functions"]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200 